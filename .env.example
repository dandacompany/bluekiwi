# Generate with: openssl rand -hex 16
DB_PASSWORD=changeme_hex16
# Generate with: openssl rand -hex 32
JWT_SECRET=changeme_hex32
APP_PORT=3100
PUBLIC_URL=http://localhost:3100
BLUEKIWI_VERSION=latest
